{% set version = "2.10.2" %}
{% set sha256 = "6d9ea49b8aab2e8c3e3def0b4bf49df6d3f7152656bdf17afe18ec5332d584fc" %}

package:
  name: django-grappelli
  version: {{ version }}

source:
  fn: django-grappelli-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/d/django-grappelli/django-grappelli-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - django

test:
  imports:
    - grappelli
    - grappelli.templatetags
    - grappelli.tests
    - grappelli.views

about:
  home: http://django-grappelli.readthedocs.org
  license: BSD 3-Clause
  license_file: LICENSE
  summary: 'A jazzy skin for the Django Admin-Interface.'

extra:
  recipe-maintainers:
    - kwilcox
    - ocefpaf
